import styled from 'styled-components';

export const Avatar = styled.img`
  width: 34px;
  border-radius: 50%;
  margin-left: 8px;
  background: #fff;
  padding: 2px;
  box-shadow: 0 3px 5px rgba(31, 32, 65, 0.2);

  &:hover {
    cursor: pointer;
  }
`;
